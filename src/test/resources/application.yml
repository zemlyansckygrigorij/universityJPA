server:
  port: ${SERVER_PORT:8080}
spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5432/universitytest
    username: postgres
    password: rfnfdfcbz123!
  jpa:
    hibernate:
      ddl-auto: update